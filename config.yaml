training:
  emb_dim: 512
  batch_size: 4096
  k_neg: 5
  learning_rate: 0.001
  epochs: 15

paths:
  checkpoint_dir: "checkpoints"
  log_file: "training_loss.json"
  results_dir: "results"

dataset:
  path: "wikimedia/wikipedia"
  name: "20231101.tr"
