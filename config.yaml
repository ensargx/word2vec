training:
  emb_dim: 512
  batch_size: 4096
  k_neg: 5
  learning_rate: 0.001
  epochs: 15

paths:
  checkpoint_dir: "checkpoints"
  log_file: "training_loss.json"
  dataset_name: "afmck/text8"
  results_dir: "results"
